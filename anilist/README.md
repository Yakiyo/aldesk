# Anilist

Simple dart library for interacting with the Anilist GraphQl api. Graphql queries/mutations are converted to 
respective dart classes/models via [Ferry Graphql](https://ferrygraphql.com/). Run the `make gen` command from root directory to run build runner